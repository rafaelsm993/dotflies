; Minimal folds — only structural blocks, matching the Apex (.cls) fold style.
; Overrides the default ecma+typescript queries which fold interfaces, enums, etc. individually.

(statement_block) @fold
(class_body) @fold
