; Minimal folds — only structural blocks, matching the Apex (.cls) fold style.
; Overrides the default ecma query which folds everything (arrays, args, arrow fns, etc.)

(statement_block) @fold
(class_body) @fold
